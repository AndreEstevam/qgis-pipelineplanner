# Allows the user to create a line and evaluate impacts

Allows the user to create a potential pipeline route by clicking on the map. When the pipeline route is finished all impacted environmental constraints will be reported.